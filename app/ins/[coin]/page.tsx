//will display price, vlume data, chart?, scrapped news

//params to get and used on-site, search-parameter also possible
export default function Page({ params } : {
    params : {coin : string}

}) {
    return <html></html>
}